import {SearchColumn} from "@components/Search/SearchComponent.tsx";

const GroupSearchColumn: SearchColumn[] = [
    { key: 'name', label: 'Name' },
    { key: 'description', label: 'Description' },
];

export default GroupSearchColumn;
